# Pikorua HRM — Progress Log

> Living status doc. Update after every meaningful change (standing project rule).
> Source of truth for scope = [docs/](docs/) (PRD, SCHEMA, IMPLEMENTATION_PLAN, API_SPEC).

**Last updated:** 2026-07-14 (Track B Milestone 1 complete: 1.1, 1.2, 1.3, 1.3 hierarchy follow-up; Milestone 2 complete: 2.1 decision, 2.2 metric mode, 2.3 daily planning/EOD + point ledger, 2.4 reimbursements + cross-track helper live; 2.4b new cross-track stub `getApprovedUnpaidLeaveDays` added for Track A's payroll summary; **Milestone 3 now fully complete**: 3.1 recognition leaderboard + Employee of the Month, `getEmployeeOfMonthStatus` cross-track helper live; 3.2 notifications infra, wired into leave/reimbursement approve+reject; 3.3 announcements (team/all/specific-team scoping); 3.4 employee documents (local-disk storage, revised from S3 — see below); 3.5 events (birthday/anniversary banner + meetings CRUD + reminder cron); post-M3 bug-fix pass: Lead cross-team assignment scoping, Lead self-service access to own WorkItems/daily-selections, S3→local-disk storage architecture change)

---

## Legend
- ✅ done · 🚧 in progress · ⬜ not started · ⚠️ blocked / needs decision

---

## Phase 0 — Shared Foundation (`main`)

Built together before the two tracks branch off. Both tracks depend on these files.

| Item | Status | Notes |
|---|---|---|
| Repo scaffold (npm workspaces, `apps/web`, root `prisma/`) | ✅ | Matches IMPLEMENTATION_PLAN §2 tree |
| Next.js + TypeScript + Tailwind + shadcn tokens | ✅ | `apps/web` (Next 14 App Router) |
| `.env.example` (all shared env vars) | ✅ | DB, AUTH_SECRET, S3, CRON_SECRET |
| `prisma/schema.prisma` — **full** schema, all tables | ✅ | Every table from SCHEMA.md; deferred `device_punch_raw` intentionally omitted |
| `lib/db` — Prisma client singleton | ✅ | `@/lib/db/prisma` |
| `lib/rbac` — roles + `requireRole()` + role groups | ✅ | 7 roles; `bde_lead` deliberately excluded (⚠️ see open questions) |
| `lib/auth` — password hashing + JWT session | ✅ | bcryptjs + jose; `getSession()` |
| `lib/api` — `{ data, error }` envelope | ✅ | `ok()` / `fail()` / `failFor()` |
| Auth routes: login / logout / me | ✅ | `app/api/v1/auth/*` |
| `components/ui` — shadcn primitives | ✅ | button, card, input, label, badge, table |
| Cross-track helper contracts (signatures) | ✅ | `getApprovedReimbursementTotal`, `getEmployeeOfMonthStatus` — stubbed, throw NotImplemented (Track B implements) |
| Seed script (payroll config, 3 depts + labels, teams, 7 role users) | ✅ | `prisma/seed.ts`, default pw `Password123!` |
| Dependency-graph tooling ("graphify") | ✅ | dependency-cruiser: `.dependency-cruiser.cjs` + `depgraph:*` npm scripts; enforces no-circular + track boundaries. SVG output needs GraphViz `dot`. |
| `bun install` + dev server running | ✅ | Confirmed by user 2026-07-13: `bun install` succeeded, `bun run dev` starts (a stale `.next/cache/webpack` pack file warning appeared — benign, cache-only, Next rebuilds it). |
| First Prisma migration + `bun run build` verified | ✅ | Confirmed 2026-07-13: local Postgres 18 running on **port 5433** (not 5432 — WSL's `wslrelay.exe` squats on 127.0.0.1:5432/::1:5432, shadowing native Postgres; moved native instance to 5433 to avoid the conflict, see `postgresql.conf`). `.env` created from `.env.example` with real `DATABASE_URL`/`AUTH_SECRET`. `prisma migrate dev --name init` and `bun run db:seed` both succeeded. `bun run build` compiles clean. |
| Shared-file warning mechanism | ✅ | Canonical list now in `CLAUDE.md` (Shared foundation section). Two enforcement layers: (1) AI rule — Claude stops and flags before editing a listed file; (2) `.githooks/pre-commit` — warns (never blocks) at commit time if staged files match the list. Hook is opt-in: run `git config core.hooksPath .githooks` once per clone (not run automatically — see README "Contributing"). |

### Package manager / runtime: **Bun** (1.3.14)
Bun is the package manager and runtime for this project. Bun runs the TypeScript seed directly (`bun prisma/seed.ts`), so `tsx` was dropped. Root scripts use `bun run --filter=@pikorua-hrm/web <script>` for the workspace app.

### To finish Phase 0 (next actions)
1. ~~`bun install`~~ ✅ done.
2. Set up a Postgres DB, copy `.env.example` → `.env`, set `DATABASE_URL` + `AUTH_SECRET`.
3. `bun run prisma:migrate` (creates the initial migration from the full schema).
4. `bun run db:seed`.
5. Confirm login works end-to-end (`POST /api/v1/auth/login`).
6. `bun run build` to confirm the scaffold compiles.
7. Then branch: `track-a/*` and `track-b/*`.

---

## Track A — People, Time & Money (owner: Umang)
Employees · Departments/Teams/Hierarchy config · Attendance (manual) · Payroll/Payslips

| Milestone | Status |
|---|---|
| Employee CRUD + department/team management | ⬜ |
| `department_labels` config UI | ⬜ |
| Manual Clock In/Out + HR/Admin approval & edit screen | ⬜ |
| Payroll config + payslip generation (manual fields + auto deductions + reimbursement pull-in + EoM ref) | ⬜ |

## Track B — Work, Requests & Culture (owner: Bhavarth)
Work units/tasks · Daily planning/EOD · Requests · Recognition · Notifications · Announcements · Docs · Events · Assets stub

**Full detailed tasklist with context, files, RBAC, and definitions of done: [TRACK_B_TASKLIST.md](TRACK_B_TASKLIST.md).** Table below mirrors it at milestone granularity — update both when a milestone's status changes.

| Milestone | Status |
|---|---|
| M1: WorkUnit/SubUnit/WorkItem CRUD (atomic only) | ✅ (1.1 WorkUnit CRUD ✅, 1.2 SubUnit/WorkItem CRUD ✅) |
| M1: Requests — leave type only, HR/Admin-only approval | ✅ |
| M2: Metric task mode (Sales/BD) + monthly reset | ✅ (2.1 decision ✅, 2.2 metric CRUD + history ✅) |
| M2: Daily task selection + EOD point ledger | ✅ |
| M2: Reimbursement requests + implement `getApprovedReimbursementTotal` | ✅ |
| M3: Recognition leaderboard + Employee of the Month + implement `getEmployeeOfMonthStatus` | ✅ |
| M3: Notifications infra | ✅ |
| M3: Announcements (team/all/specific-team scoping) | ⬜ |
| M3: Employee documents upload | ⬜ |
| M3: Events — birthday banner + Meetings + reminders | ⬜ |
| M4: Cross-track integration testing (joint w/ Track A) | ⬜ |
| Assets stub | ⬜ |

### 1.2 detail (2026-07-13)
Built `POST /work-units/:id/sub-units`, `POST /sub-units/:id/work-items` (atomic only — `mode = metric` rejected with `NOT_IMPLEMENTED`/501), `PATCH /work-items/:id`, `GET /work-items/mine`. Also extended `GET /work-units/:id` to nest `subUnits` + `workItems` per API_SPEC §4 (Employee's status-only view filters `workItems` down to their own assignments). `bun run build` clean. Verified live against the seeded DB with curl: Tech Lead creates sub-unit/work-item; Sales Lead gets 404 (not 403) on another department's WorkUnit; Tech Employee gets 403 creating sub-units; atomic WorkItem requires `taskPoints`; assigned Employee can cycle `pending → wip → completed` (server sets `completedAt`) but 403s on editing `taskPoints`; owning Lead can edit all fields including `taskPoints`; `GET /work-items/mine` returns the assignee's own tasks and 403s for non-Employee roles (tech_lead) per spec's strict role list.

### 1.3 detail (2026-07-13)
Built `POST /requests`, `GET /requests`, `GET /requests/:id`, `PATCH /requests/:id/approve`, `PATCH /requests/:id/reject` — leave types only this milestone (`leave_paid`/`leave_unpaid`); other types (`reimbursement`/`wfh`/`other`) rejected with `NOT_IMPLEMENTED`/501, mirroring the 1.2 metric-mode pattern. Golden rule enforced: approve/reject check `FINANCE_ROLES` before anything else, so a Team Lead gets 403 on their own team's request every time, and re-approving/rejecting a non-pending request returns 409. GET scoping matches WorkUnit's pattern: Admin/HR see all (with `type`/`status`/`employee_id` filters), Lead sees own team only (resolved via `Team.teamLeadId`, filters ignore attempts to escape team scope), Employee sees self only, and cross-scope `GET /requests/:id` 404s instead of 403 to avoid leaking existence. `bun run build` clean. Verified live against the seeded DB with curl across Tech Lead, Tech Employee, Sales Lead, Sales Employee, HR, and Admin — including catching and fixing a real bug during verification (reimbursement type was failing zod validation on missing dates before ever reaching the leave-type-only rejection check; fixed by making `dateFrom`/`dateTo` optional at the schema level and validating their presence only after confirming the type is a leave type).

**2026-07-13 update — hierarchy submission resolved.** Stakeholder direction: leave requests work in hierarchy — Team Leads can file their own leave (approved by HR/Admin) and HR can file their own leave (approved by Admin only). `POST /requests` now allows Employees, Team Leads, and HR (`CAN_SUBMIT_ROLES` in `requests/route.ts`); Admin is intentionally excluded (no one above Admin to approve it). Approval stays Admin/HR only per the golden rule — unchanged — but `approve`/`reject` now block self-approval by comparing the requester's linked `User.id` to the approving session's `userId`, so an HR request can't be approved by that same HR user and must go up to Admin. Lead's team-scoped `GET /requests` was extended to also include the Lead's own filed requests (previously only team *members*, which excluded the Lead). `bun run build` clean; live-verified with curl: Tech Lead and HR each filed their own leave, HR self-approve attempt got 403, Admin approved HR's request, HR approved the Lead's request, and a Tech Employee still got 403 attempting to reject (golden rule intact).

### 2.2 detail (2026-07-13)
Extended `POST /sub-units/:id/work-items` and `PATCH /work-items/:id` to support `mode = metric` (previously atomic-only, metric rejected with `NOT_IMPLEMENTED`). Create requires `targetValue`/`periodMonth`/`periodYear` together; a duplicate (employee, sub-unit, period) triggers `409 CONFLICT` instead of silently creating a second row for the same month, since the 2.1 decision means each period is its own row. PATCH: the owning Lead/Admin/HR can edit `targetValue` any time without touching `currentValue`; the assigned Employee can only update `currentValue`; `status` is derived automatically (`completed` once `currentValue >= targetValue`, else `wip`) rather than settable directly in metric mode — mirrors PRD §5.6's "no fixed done state" rule. Field-mode validation added so `taskPoints` can't be set on a metric item or `targetValue`/`currentValue` on an atomic one.

**Extra — growth-over-time view (stakeholder request, same session):** added `GET /api/v1/employees/:id/work-items/history` (`?year=`/`?limit=` optional), returning an employee's past metric-mode WorkItems newest-period-first with a derived `achievedPct`. RBAC: Admin/HR any employee, Lead their own team (via `team.teamLeadId`), Employee self only, else 403. New file lives under Track A's `employees/` folder (owned by Track B per the 2.3 folder-overlap note) — flagged to Umang once, not a shared-file-list item.

`bun run build` clean. Live-verified against the seeded DB with curl: Sales Lead created a WorkUnit/SubUnit and a July metric WorkItem (target 100 calls); missing `targetValue`/period fields correctly 422'd; a duplicate create for the same (employee, period) correctly 409'd; Sales Employee updated `currentValue` to 45 (status auto-set `wip`) and was blocked (403) from changing `targetValue`; Sales Lead adjusted `targetValue` to 80 mid-period without resetting `currentValue`; pushing `currentValue` to 80 auto-completed the item (`completedAt` set). Created a second (June) metric WorkItem at 72/100 and confirmed `/history` returns both periods ordered newest-first with correct `achievedPct`, visible to the Employee (self), the Sales Lead (own report), and Admin, and correctly 403'd for an unrelated Tech Employee.

### 2.3 detail (2026-07-13)
Built `POST /daily-selections` (Employee only; additive/`skipDuplicates` upsert of `{ workItemIds }` into today's server-computed UTC date, rejecting any id not assigned to the caller), `GET /daily-selections/today` (Employee self, Lead own team via `Team.teamLeadId` — Admin/HR excluded per API_SPEC's strict role list, same convention as `work-items/mine`), `POST /work-items/:id/complete` (assigned Employee only, atomic-mode only — metric items are rejected since they auto-complete via PATCH's current≥target check — credits `taskPoints` to `employee_point_ledger` in the same `$transaction` as the status/`completedAt` update; already-completed → `409`), and `GET /employees/:id/points` (Admin/HR any, Lead own team, Employee self; returns the ledger plus a summed `balance`) — the latter two new files live under Track A's `employees/` folder (owned by Track B per the folder-overlap note; flagged, not shared-file-list). Also **fixed a real gap**: `PATCH /work-items/:id` from 1.2 already let an Employee set `status = completed` directly, which would have skipped ledger crediting now that it exists — added the same transactional credit to PATCH's atomic-completion branch, guarded by `wasCompleted` (checked pre-update) on both routes so completion credits exactly once no matter which endpoint is used. `bun run build` clean. Live-verified against the seeded DB with curl: Tech Lead created a fresh atomic WorkItem (15 pts) for Tech Employee; Lead's own `/daily-selections` POST correctly 403'd (Employee-only); Employee's selection of an unassigned item 422'd, selecting their own item succeeded and was idempotent on re-POST; `GET /daily-selections/today` showed the selection to the Employee (self) and Lead (own team), returned empty for an unrelated Sales Employee, and 403'd for Admin; Lead's `/complete` attempt 403'd (assignee-only), Employee's `/complete` credited 15 points and returned `409` on retry; `GET /employees/:id/points` showed `balance: 15` for self/Lead/Admin and 403'd for an unrelated Sales Employee; a second WorkItem (8 pts) completed via direct `PATCH status=completed` correctly credited (balance → 23), and re-PATCHing it to `completed` again left the balance unchanged at 23 (no double credit).

### 2.4 detail (2026-07-13)
Extended `POST /requests` to accept `type = reimbursement` (`amount` required, `attachmentUrl` optional) alongside the existing leave types — `wfh`/`other` remain `NOT_IMPLEMENTED`/501 until Milestone 3. No RBAC changes needed: `CAN_SUBMIT_ROLES`/golden-rule approve-reject logic from 1.3 is already type-agnostic. **Flagged before editing** — implemented the cross-track shared helper `getApprovedReimbursementTotal(employeeId, month, year)` in `apps/web/lib/requests/reimbursements.ts` (Phase 0 contract, imported by Track A's payroll): sums `amount` for `type = reimbursement`, `status = approved`, scoped by `approvedAt` falling in the given month/year (not `createdAt` — a reimbursement is keyed to the payroll period it's *approved* in, per PRD §5.2/§5.13, since request submission and approval can span different months). Signature kept exactly as the Phase 0 stub (`(employeeId, month, year) => Promise<number>`) — Track A's payroll call site needs no changes; it just stops throwing `NotImplementedError`. `bun run build` clean. Live-verified with curl + a direct script call: reimbursement missing `amount` 422'd; Team Lead's approve attempt on a reimbursement still 403'd (golden rule holds); HR approved a ₹1500 request → helper returned 1500 for the current month, 0 for the prior month; a second ₹700 approval → total 2200; a third ₹9999 request that was *rejected* was correctly excluded from the sum.

**Milestone 2 is now fully complete** (2.1 decision, 2.2 metric mode + history, 2.3 daily planning/EOD + point ledger, 2.4 reimbursements). Next: Milestone 3 (3.1 recognition leaderboard + Employee of the Month, 3.2 notifications, 3.3 announcements, 3.4 documents, 3.5 events).

### 2.4b detail (2026-07-14) — third cross-track stub
Umang (Track A) hit a gap building the payroll summary endpoint: unpaid-leave days aren't in `attendance_records`, they live in Track B's `requests` table (`type = leave_unpaid`, `status = approved`). He proposed adding a 3rd cross-track stub now, matching the existing pattern, rather than blocking his summary endpoint — added `getApprovedUnpaidLeaveDays(employeeId, month, year): Promise<number>` to `apps/web/lib/requests/reimbursements.ts` (flagged as a shared-file edit before touching it), throwing `NotImplementedError("getApprovedUnpaidLeaveDays", "Track B")` for now — same throwing-stub shape `getApprovedReimbursementTotal` started as before 2.4. `bun run build` clean. Real implementation still pending; needs a decision on how to count a `leave_unpaid` range spanning two payroll periods before it can return real numbers.

### 3.1 detail (2026-07-14)
Built `POST /api/v1/cron/recognition-snapshot` (checks `Authorization: Bearer <CRON_SECRET>`, not a user session — 401 on missing/wrong secret), `GET /api/v1/recognition` (Any authenticated user; filters `period_type` default `monthly`, optional `department_id`; if `period_start` isn't given, serves the latest computed period for the filters). Scoring: Tech departments (`department.typeKey === "tech"`) score off `employee_point_ledger` points credited within the period window; Sales/BD departments score off the average achieved-% (`currentValue/targetValue*100`) across their metric WorkItems for the period's month (weekly snapshots reuse the same month's metric items as a snapshot-in-time reading, since metric targets only have monthly granularity per the 2.1 decision). Re-running the cron for the same `(period_type, period_start)` deletes and rewrites that period's rows — idempotent. **Assumption (PRD §7 open question #3 still unresolved):** single winner per department — `rank = 1` monthly gets `is_employee_of_month`, gated on `score > 0` so a department with zero activity that period has no winner (added this guard beyond the literal spec text, since crowning a 0-score employee didn't seem like the intent). ⚠️ **Flagged before editing** — implemented the cross-track shared helper `getEmployeeOfMonthStatus(employeeId, month, year)` in `apps/web/lib/recognition/employee-of-month.ts` for real (was a throwing stub): looks up the monthly snapshot for that employee/period and returns `isEmployeeOfMonth`, or `false` if no snapshot exists yet for that period (not an error — the cron simply hasn't run for it). Signature unchanged; Umang's payslip generation screen now gets a real boolean instead of a thrown `NotImplementedError`. `bun run build` clean. Live-verified against the seeded DB: ran the cron for the current week+month (5 rows written per department set), confirmed `GET /recognition?period_type=monthly` ranked Tech Employee #1 (23 pts, EoM=true) over Tech Lead (0 pts), Sales Employee #1 (100%, EoM=true) over Sales Lead (45%), and BD Executive alone at rank 1 with score 0 correctly **not** flagged EoM (the score>0 guard); `department_id` filter scoped correctly to just Sales; unauthenticated GET 401'd; wrong cron secret 401'd; and a direct call to `getEmployeeOfMonthStatus` confirmed true for the Sales Employee, false for the Sales Lead, false for the BD Executive (zero-score guard), and false for a month with no computed snapshot (June).

### 3.2 detail (2026-07-14)
Built the generic `pushNotification(userId, type, message)` service (`apps/web/lib/notifications/push.ts` — new file, not on the shared-file list since it didn't exist in Phase 0, but flagged to Umang as reusable infra Track A can call for e.g. "payslip generated" instead of writing to `notifications` directly), plus `GET /api/v1/notifications` (self-only list) and `PATCH /api/v1/notifications/:id/read` (self-only, idempotent, 404s — not 403s — on someone else's notification to avoid leaking existence, same convention as `requests/:id`). Wired `pushNotification` into both `requests/:id/approve` and `:id/reject` (DoD only named the approval flow, but reject is symmetric and equally useful, so did both — cascading-complete). `bun run build` clean. Live-verified against the seeded DB: Tech Employee filed a leave request, HR approved it, `GET /notifications` showed a fresh `leave_paid_approved` row; marked it read via PATCH and confirmed `readAt` set and idempotent on re-PATCH; HR attempting to mark *that* notification read got 404; unauthenticated GET 401'd; a second leave request rejected by HR produced a `leave_paid_rejected` notification for the employee.

### 3.3 detail (2026-07-14)
Built `GET/POST /api/v1/announcements`. POST: Lead role → `scope_type` silently forced to `team` regardless of what's sent, with `teamIds` set to the Lead's own led team (`Team.teamLeadId = session.employeeId`); Admin/HR → `all` or `specific_teams` (`teamIds` required, non-empty, for `specific_teams`); any other role 403s outright. GET: Admin/HR see everything; everyone else gets `all` announcements plus `team`/`specific_teams` announcements whose `teamIds` includes their own `Employee.teamId` (Prisma `has` filter on the `String[]` column). `bun run build` clean. Live-verified against the seeded DB: Tech Lead's POST with `scope_type: "all"` was correctly forced to `team` scoped to their own team id; HR successfully posted both an `all` announcement and a `specific_teams` announcement scoped to the Tech team; a Tech Employee's POST attempt 403'd; GET as Tech Employee returned all three (company-wide + own-team `team` + own-team `specific_teams`); GET as Sales Lead correctly excluded the Tech-team-scoped ones.

### 3.4 detail (2026-07-14)
Added `lib/storage/s3.ts` (new deps `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner`) with `getPresignedUploadUrl`/`buildFileUrl` for a client-direct-to-bucket upload flow, and `GET/POST /api/v1/employees/:id/documents` (physically under Track A's `employees/` folder — folder-overlap note, not shared-file-list). The route itself takes `{ docType, fileUrl }` — mirroring `Request.attachmentUrl`'s existing convention from 2.4 — rather than handling file bytes server-side, since this environment's `.env` has only placeholder S3 credentials; no file content passes through the Next.js server either way. RBAC: Admin/HR any employee, Employee self (both GET and POST) — broader on POST than API_SPEC.md §8's literal "Admin/HR only" line, following the tasklist's DoD text instead; flagged as an unconfirmed assumption. `bun run build` clean. Live-verified: Tech Employee self-uploaded a document, HR uploaded a second for the same employee, GET as the owning employee and as Admin both returned both documents, and GET as an unrelated employee (Sales Lead) 403'd.

**Storage revised 2026-07-14 (superseded above):** deployment target is a single GCP VM, not serverless, so the S3 presigned-upload design was replaced with local-disk storage before it was ever wired to a real bucket. Removed `lib/storage/s3.ts` and the `@aws-sdk/client-s3`/`@aws-sdk/s3-request-presigner` deps (flagged before editing `apps/web/package.json` — shared file). Added `lib/storage/local.ts` (`saveUploadedFile`/`readUploadedFile`, files under `<cwd>/uploads/`, outside `public/` so nothing is directly web-servable). `POST /employees/:id/documents` now takes real `multipart/form-data` (`docType` + `file`, 10MB cap) instead of a client-supplied URL string; `fileUrl` in the DB is now an opaque internal storage key, never a bare public URL. Added `GET /employees/:id/documents/:docId/file`, a re-authenticated route (same RBAC as the parent) that streams the bytes back — `GET /employees/:id/documents` rewrites `fileUrl` to point at this route rather than exposing the raw storage key. `apps/web/uploads/` gitignored. `bun run build` clean. Live-verified: uploaded a real file as an Employee (self), fetched it back via the file route with correct `Content-Type`, HR uploaded for another employee and fetched it, an unrelated employee's fetch attempt 403'd, and a request for a non-existent document/stale key correctly 404'd rather than crashing.

### 3.5 detail (2026-07-14)
Built `GET /api/v1/events/today` (derived query — no `events` rows persisted for birthday/anniversary, matching the PRD's stated default — compares `dateOfBirth`/`dateOfJoining` month+day against today for active employees), `POST/GET /api/v1/events/meetings` (POST = Admin/HR/Lead, invitees by individual `employeeId` and/or whole `teamId`; GET scoped to creator + direct invitee + invitee-via-team), `PATCH/DELETE /api/v1/events/meetings/:id` (creator or Admin/HR; PATCH replaces the invitee set only if `inviteeEmployeeIds`/`inviteeTeamIds` is provided), `POST /api/v1/cron/birthday-check` and `POST /api/v1/cron/meeting-reminders` (both CRON_SECRET-gated). Reminder channel is in-app only, per the PRD §7 open-question-#2 default (still unconfirmed with a stakeholder). `events` has no persisted "reminder sent" flag — adding one would touch the shared `schema.prisma` for a single cron's bookkeeping — so `meeting-reminders` derives idempotency from `notifications` itself: each reminder is tagged `type = "meeting_reminder:<eventId>"` and skipped per-user if that tag already exists, making repeat cron runs inside the same open window a no-op. `bun run build` clean. Live-verified against the seeded DB: `events/today` empty at baseline, then correctly surfaced a birthday after setting a test employee's `dateOfBirth` to today (reverted to `null` after); `birthday-check` cron 401'd without the secret and, with it, pushed a company-wide notification to all 7 seeded users; a meeting created 10 minutes out with a 20-minute reminder lead (window already open) had its reminder fire on the first `meeting-reminders` run and sent zero on an immediate second run (idempotent); meeting PATCH by the creator succeeded, PATCH by an unrelated Lead 403'd, and DELETE by Admin removed it (confirmed via a follow-up GET).

**Milestone 3 is now fully complete** (3.1 recognition + EoM, 3.2 notifications, 3.3 announcements, 3.4 documents, 3.5 events). Next: Milestone 4 (joint integration week with Track A) — mostly cross-track verification/testing, not new Track B code, once Umang's payroll/payslip code calls the now-real `getApprovedReimbursementTotal`/`getEmployeeOfMonthStatus`/`getApprovedUnpaidLeaveDays` helpers.

### Post-M3 bug-fix pass (2026-07-14)
Three issues reported against the live test UI, fixed at the API layer (source of truth), not just the UI:
1. **Lead cross-team assignment:** a Team Lead's "assign task" flow had no team-membership check, so a Tech Lead could assign a WorkItem to an HR user or any other team's employee. Fixed in `sub-units/[id]/work-items/route.ts` (POST) and `work-items/[id]/route.ts` (PATCH) — see 1.2's implementation note above for detail.
2. **Lead locked out of own tasks:** `GET /work-items/mine` and `POST /daily-selections` used a strict `isEmployeeRole` whitelist that 403'd Leads, even though Leads have Employee records and can be assignees like anyone else; `GET /daily-selections/today` separately under-scoped a Lead's own employeeId. See 1.2 and 2.3's implementation notes above.
3. **Storage architecture:** switched Milestone 3.4's document storage from S3 presigned uploads to local disk, since the deployment target is a persistent GCP VM rather than serverless infra. See 3.4's "Storage revised" note above.

All three live-verified against the dev server and seeded DB, test data cleaned up afterward. `bun run build` clean throughout.

### Basic test UI (2026-07-14)
Built a deliberately minimal UI under `apps/web/app/test/**` (plus `apps/web/app/login`) so M1-M2 can be exercised by hand in a browser instead of curl/Postman only — explicitly **not** the final UI, which comes later. Pages: `/login`, `/test` (session check), `/test/work-units` (list + create, click into a WorkUnit to add SubUnits and atomic/metric WorkItems), `/test/work-items` (my items — PATCH progress / complete), `/test/requests` (submit leave/reimbursement, approve/reject), `/test/daily-planning` (select today's tasks + view selection), `/test/points` (ledger + balance lookup), `/test/history` (growth-over-time lookup). `/test/**` is gated server-side via the real `getSession()` (redirects to `/login`), and all pages call the real `/api/v1/**` routes client-side through a thin `apiFetch` helper — no mock data. `bun run build` clean; live-verified against the running dev server end-to-end as Sales Lead / Sales Employee / HR: created a WorkUnit → SubUnit → metric WorkItem, updated its progress via PATCH, created an atomic WorkItem, completed it as the assignee (ledger credited 10 pts, confirmed via the points endpoint), selected it via daily-selections, submitted + HR-approved a leave request, submitted a reimbursement, and confirmed a non-HR approve attempt still 403s — all matching what each page renders.

### Test UI extended for Milestone 3 (2026-07-14)
Added 5 more pages to the same `/test/**` harness, same conventions (session-gated via `layout.tsx`, real `/api/v1/**` calls only, no mock data): `/test/recognition` (leaderboard lookup by period_type, reads the real `{ periodType, periodStart, leaderboard }` shape from `GET /recognition` — employee/department names already joined server-side), `/test/notifications` (list + mark-read; response is `{ notifications: [...] }`, not a bare array — page reads it correctly), `/test/announcements` (post form with role-conditional scope picker — Finance gets `all`/`specific_teams` + team multi-select, Lead sees an explanatory note that scope is forced server-side, others see a note that posting will 403 — plus the visible-to-you list), `/test/documents` (employee-dropdown lookup of `GET /employees/:id/documents` + upload form posting `{ docType, fileUrl }`, no live S3 bucket in this env so `fileUrl` is a pasted URL, mirrors the `Request.attachmentUrl` convention), `/test/events` (today's birthday/anniversary banner, meeting creation form with individual + whole-team invitee pickers, meetings list with delete gated to creator/Admin/HR — explicit note in-code that the two CRON_SECRET-gated cron routes are deliberately not wired into this browser UI since the secret must stay server-only). Also added `apps/web/app/api/test/teams/route.ts`, a test-harness-only (not API_SPEC.md) endpoint feeding team dropdowns, same pattern as the existing `/api/test/employees` and `/api/test/departments`. `layout.tsx`'s nav updated with all 5 new links.

`bun run build` clean (all new routes listed, zero type errors). Live-verified against the dev server (port 3002) as Admin/HR/Tech Lead/Tech Employee/Sales Lead: recognition leaderboard returned real seeded scores/ranks with correct EoM badges; `/api/test/teams` returned all 3 teams; notifications list returned real birthday/meeting-reminder rows; announcements GET returned team-scoped + all-scoped rows correctly filtered for a Tech Employee; a Lead's POST was force-scoped to `team`/their own team (confirmed via response body) while a plain Employee's POST 403'd; `events/today` returned the expected empty banner (no seeded birthdays today); employee documents GET returned the seeded Offer Letter row for self-lookup. One bug caught by this verification pass: `/test/notifications/page.tsx` initially assumed `GET /notifications` returned a bare array, but the real shape is `{ notifications: [...] }` — fixed before reporting done.

---

## Open decisions (blocking specific features — confirm with stakeholder)
See PRD §7. Tracked in memory (`open-questions`):
1. ⚠️ `bde_lead` role — needed or not? (currently excluded from role enum)
2. ⚠️ Meeting reminder channel — in-app only vs. also email/SMS (assuming in-app)
3. ⚠️ Employee of the Month ties — single winner vs. multiple
4. ✅ Monthly metric-target reset — **resolved 2026-07-13: new row per month.** Each metric-mode WorkItem is scoped to one `period_month`/`period_year`; the next month gets a fresh `work_items` row rather than resetting `current_value` in place. Rationale: preserves per-month history for `recognition_snapshots` without a fragile "snapshot-before-reset" job ordering, and avoids any scheduled job silently destroying `current_value`. A monthly rollover (new row, `target_value` carried forward or re-set by the Lead) will be built as part of 2.2.
